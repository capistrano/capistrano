#
# This is the gemfile for local development
# these are the requirements for running
# the test suite.
#

source :gemcutter

gem 'net-ssh'
gem 'net-ssh-gateway'
gem 'net-sftp'
gem 'net-scp'
gem 'highline'

group :test do
  gem "mocha"
end
